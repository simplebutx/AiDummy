# Frontend Demo

고객 문의 분류 결과를 확인하는 React 프론트엔드입니다.

동작 흐름:

`frontend -> Spring Boot Backend (/api/ai/**) -> FastAPI ai server`

## 실행

```bash
npm install
npm run dev
```

기본 백엔드 주소:

`http://localhost:8080`
