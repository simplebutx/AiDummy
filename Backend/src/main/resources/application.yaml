spring:
  application:
    name: AiDummy
  datasource:
    url: jdbc:mysql://localhost:3306/aidummy
    username: root
    password: giants49
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret:
  access-token-expiration-ms: 3600000

ai:
  service:
    base-url: http://localhost:8000
